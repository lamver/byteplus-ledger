# Что доступно на BytePlus ModelArk

Разбор личного аккаунта (Personal User, регион Asia Pacific (Johor), USD),
состояние на 2026-08. Цифры получены из собственного биллинга и реальными
запросами к API, а не из публичного прайса, поэтому учитывают фактические
ставки аккаунта. У вас может отличаться, проверьте через `report.py`.

## Бесплатные пакеты

Выдаются при активации модели, срок жизни примерно два года.

| Пакет | Объём |
|---|---|
| DeepSeek-V4-pro-0813 | 500K токенов |
| DeepSeek-V4-pro | 500K токенов |
| DeepSeek-V4-flash-0731 | 500K токенов |
| GLM-5.2 | 500K токенов |
| Seedance-1.5-pro (видео) | 2 000K токенов |
| Seedream 4.5 (изображения) | 200 штук |
| Seedream 5.0-Lite | 50 штук |

Итого около 4 млн бесплатных токенов на текстовые модели плюс
250 изображений. Пакет привязан к конкретной версии модели: активация
новой ревизии даёт новые 500K.

Проверить остаток: `python quota.py`.

## Подписка Coding Plan

**Coding-Plan-Lite (Monthly), 10 USD в месяц.**

Важное свойство, которое неочевидно при покупке: план не даёт безлимита.
Он открывает доступ к моделям, но расход всё равно идёт из пакетов по
500K на модель. Когда пакет кончается, включается поштучная оплата, и
счёт приходит сверх подписки.

## Цены за пределами пакетов

За 1K токенов, USD:

| Модель | Prompt | Completion | KV-cache hit |
|---|---|---|---|
| DeepSeek-V4-pro-0813 | 0.001320 | 0.003960 | 0.000044 |
| DeepSeek-V4-pro | 0.001740 | 0.003480 | — |
| DeepSeek-V4-flash-0731 | 0.000440 | 0.001320 | — |
| GLM-5.2 | 0.001400 | 0.004400 | 0.000260 |

| Прочее | Цена |
|---|---|
| Seedance-1.5-pro, видео без звука | 0.001200 / 1K токенов |
| Seedance-1.5-pro, видео со звуком | 0.002400 / 1K токенов |
| Seedream 4.5, генерация или правка | 0.040000 / изображение |

Ориентиры: DeepSeek-V4-flash примерно втрое дешевле pro на входе.
KV-cache hit у DeepSeek в шесть раз дешевле, чем у GLM (0.000044 против
0.000260), и на длинном контексте это решает.

Осторожно с видео: 80 млн токенов Seedance это порядок 1.8 USD сверх
пакета, генерация видео съедает квоту несопоставимо быстрее текста.

## Модели, открытые по API

Из 55 позиций каталога живых 43, текстовых и мультимодальных 23.
На конкретном аккаунте реально отвечали на запрос четыре:

| Модель | Контекст | Max output |
|---|---|---|
| `deepseek-v4-pro-ga-260813` | 1 048 576 | 393 216 |
| `deepseek-v4-flash-ga-260731` | 1 048 576 | 393 216 |
| `deepseek-v4-pro-260425` | 1 048 576 | 393 216 |
| `glm-5-2-260617` | 1 048 576 | 131 072 |

Остальные отдают `ModelNotOpen` и требуют активации в консоли Ark:
`seed-2-0-pro`, `seed-2-0-code-preview`, `seed-2-0-lite`, `seed-2-0-mini`,
`seed-1-8`, `seed-1-6` и flash-версии, `dola-seed-2-1-turbo`, `glm-4-7`,
`gpt-oss-120b`, `deepseek-v3-2`, `seed-translation`.

Проверить свой список: `python report.py --probe`.

## Что выбрать для кода

**`deepseek-v4-pro-ga-260813`.** Проверено `bench.py` на трёх задачах
(генерация, поиск бага, алгоритм с краевыми случаями): 3 из 3 с первой
попытки, около 0.7K токенов на задачу, 12-20 секунд на ответ. Минус в
скорости: много reasoning-токенов, ответ заметно медленнее обычного.

`deepseek-v4-flash-ga-260731` втрое дешевле, годится для массовых мелких
правок и там, где не нужен сильный reasoning.

GLM-5.2 менее выгоден: дороже на выходе, KV-cache в шесть раз дороже.

## Как читать биллинг

Полезные поля `ListBillDetail`:

- `DeductionCount` больше нуля — расход покрыт пакетом, денег не стоит
- `PretaxAmount` больше нуля — оплачено деньгами
- `BillingMode = Yearly/monthly` — подписка или выдача пакета
- `BillingMode = Pay-as-you-go` — поштучный расход
- `BillCategory = Consumption-purchase` — выдача пакета
- `BillCategory = Consumption-usage` — расход

Признак исчерпания пакета: `DeductionCount` перестал расти, а
`PretaxAmount` пошёл вверх.
